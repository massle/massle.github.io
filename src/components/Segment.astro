---
const { id, title } = Astro.props;
---
<div class="mt-5">
  <a href="#" id={id} style="visibility: hidden">{title}</a>
  <div class="text-3xl w-full font-bold mb-5">{title}</div>
  <slot />
</div>

